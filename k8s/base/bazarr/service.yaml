apiVersion: v1
kind: Service
metadata:
  name: bazarr
  namespace: media
  labels:
    app.kubernetes.io/name: bazarr
    app.kubernetes.io/part-of: media-stack
spec:
  selector:
    app: bazarr
  type: ClusterIP
  ports:
  - name: http
    port: 6767
    targetPort: 6767
