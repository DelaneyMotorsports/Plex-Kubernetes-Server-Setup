apiVersion: v1
kind: Service
metadata:
  name: posterr
  namespace: media
  labels:
    app.kubernetes.io/name: posterr
    app.kubernetes.io/part-of: media-stack
spec:
  selector:
    app: posterr
  type: ClusterIP
  ports:
  - name: http
    port: 3000
    targetPort: 3000
