apiVersion: v1
kind: Service
metadata:
  name: radarr
  namespace: media
  labels:
    app.kubernetes.io/name: radarr
    app.kubernetes.io/part-of: media-stack
spec:
  selector:
    app: radarr
  type: ClusterIP
  ports:
  - name: http
    port: 7878
    targetPort: 7878
