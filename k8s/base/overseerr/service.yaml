apiVersion: v1
kind: Service
metadata:
  name: overseerr
  namespace: media
  labels:
    app.kubernetes.io/name: overseerr
    app.kubernetes.io/part-of: media-stack
spec:
  selector:
    app: overseerr
  type: ClusterIP
  ports:
  - name: http
    port: 5055
    targetPort: 5055
