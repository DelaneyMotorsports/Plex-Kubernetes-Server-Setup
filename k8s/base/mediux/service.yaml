apiVersion: v1
kind: Service
metadata:
  name: mediux
  namespace: media
  labels:
    app.kubernetes.io/name: mediux
    app.kubernetes.io/part-of: media-stack
spec:
  selector:
    app: mediux
  type: ClusterIP
  ports:
  - name: http
    port: 80
    targetPort: 80
