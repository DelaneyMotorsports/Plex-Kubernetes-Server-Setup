apiVersion: apps/v1
kind: Deployment
metadata:
  name: mediux
  namespace: media
  labels:
    app.kubernetes.io/name: mediux
    app.kubernetes.io/part-of: media-stack
    app.kubernetes.io/component: artwork
spec:
  replicas: 1
  strategy:
    type: Recreate
  selector:
    matchLabels:
      app: mediux
  template:
    metadata:
      labels:
        app: mediux
        app.kubernetes.io/part-of: media-stack
    spec:
      securityContext:
        fsGroup: 1000
      containers:
      - name: mediux
        image: mediux/app:latest
        envFrom:
        - configMapRef:
            name: media-stack-config
        ports:
        - name: http
          containerPort: 80
        volumeMounts:
        - name: config
          mountPath: /config
        resources:
          requests:
            cpu: "50m"
            memory: "64Mi"
          limits:
            cpu: "200m"
            memory: "256Mi"
      volumes:
      - name: config
        persistentVolumeClaim:
          claimName: mediux-config-pvc
