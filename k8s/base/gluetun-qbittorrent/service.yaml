apiVersion: v1
kind: Service
metadata:
  name: qbittorrent
  namespace: media
  labels:
    app.kubernetes.io/name: qbittorrent
    app.kubernetes.io/part-of: media-stack
spec:
  selector:
    app: gluetun-qbittorrent
  type: ClusterIP
  ports:
  - name: webui
    port: 8080
    targetPort: 8080
