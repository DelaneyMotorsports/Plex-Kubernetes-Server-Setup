apiVersion: v1
kind: Service
metadata:
  name: profilarr
  namespace: media
  labels:
    app.kubernetes.io/name: profilarr
    app.kubernetes.io/part-of: media-stack
spec:
  selector:
    app: profilarr
  type: ClusterIP
  ports:
  - name: http
    port: 6868
    targetPort: 6868
