apiVersion: v1
kind: Service
metadata:
  name: scannarr
  namespace: media
  labels:
    app.kubernetes.io/name: scannarr
    app.kubernetes.io/part-of: media-stack
spec:
  selector:
    app: scannarr
  type: ClusterIP
  ports:
  - name: http
    port: 8080
    targetPort: 8080
