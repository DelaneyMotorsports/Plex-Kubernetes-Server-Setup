apiVersion: v1
kind: Service
metadata:
  name: sonarr
  namespace: media
  labels:
    app.kubernetes.io/name: sonarr
    app.kubernetes.io/part-of: media-stack
spec:
  selector:
    app: sonarr
  type: ClusterIP
  ports:
  - name: http
    port: 8989
    targetPort: 8989
