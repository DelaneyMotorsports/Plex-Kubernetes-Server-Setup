apiVersion: v1
kind: Service
metadata:
  name: tdarr
  namespace: media
  labels:
    app.kubernetes.io/name: tdarr
    app.kubernetes.io/part-of: media-stack
spec:
  selector:
    app: tdarr
  type: ClusterIP
  ports:
  - name: webui
    port: 8265
    targetPort: 8265
  - name: server
    port: 8266
    targetPort: 8266
